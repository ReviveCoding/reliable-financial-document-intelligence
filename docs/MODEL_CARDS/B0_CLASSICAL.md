# Model Card — B0 Classical

Anchored regular expressions extract six fields from OCR-like text; deterministic parsers normalize amounts, currencies, and dates. It runs on CPU and never guesses ambiguous dates or currencies. Evidence is limited to highly regular synthetic text. It is unsuitable as a production model and has not been evaluated on OCR output, images, tables, or public benchmarks.
